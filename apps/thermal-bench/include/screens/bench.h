#pragma once

#include "screen.h"

// bench - the app's main screen. a full-screen stress canvas behind a
// semi-transparent overlay carrying the live readouts and the graph.
extern Screen benchScreen;
