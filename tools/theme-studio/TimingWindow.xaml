<Window x:Class="ThemeStudio.TimingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="When this picture shows" Width="520" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="#1e1e1e" ShowInTaskbar="False">
  <StackPanel Margin="{StaticResource PadPage}">
    <TextBlock x:Name="headingLabel" Style="{StaticResource Heading}"/>
    <TextBlock x:Name="hintLabel" Style="{StaticResource Hint}" Margin="0,0,0,16"/>

    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="70"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="16"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" Grid.Column="1" x:Name="firstColumnLabel" Style="{StaticResource Label}"/>
      <TextBlock Grid.Row="0" Grid.Column="3" x:Name="secondColumnLabel" Style="{StaticResource Label}"/>

      <TextBlock Grid.Row="1" Grid.Column="0" Text="From"/>
      <ComboBox Grid.Row="1" Grid.Column="1" x:Name="fromFirstBox" Margin="0,0,0,12"
                SelectionChanged="onAnyChanged"/>
      <ComboBox Grid.Row="1" Grid.Column="3" x:Name="fromSecondBox" Margin="0,0,0,12"
                SelectionChanged="onAnyChanged"/>

      <TextBlock Grid.Row="2" Grid.Column="0" Text="Until"/>
      <ComboBox Grid.Row="2" Grid.Column="1" x:Name="untilFirstBox" SelectionChanged="onAnyChanged"/>
      <ComboBox Grid.Row="2" Grid.Column="3" x:Name="untilSecondBox" SelectionChanged="onAnyChanged"/>
    </Grid>

    <TextBlock x:Name="summaryLabel" Style="{StaticResource Hint}" Margin="0,16,0,16"/>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" Click="onCancel" IsCancel="True"/>
      <Button x:Name="acceptButton" Content="Set" Click="onAccept" Margin="0" IsDefault="True"
              IsEnabled="False"/>
    </StackPanel>
  </StackPanel>
</Window>
